import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.appcoordenadas',
  appName: 'appcoordenadas',
  webDir: 'www/browser',
};

export default config;
